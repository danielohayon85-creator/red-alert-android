package com.shomerapp.alerts.ui.theme

import androidx.compose.ui.unit.dp

/** Shared spacing scale — every screen should pick from here instead of ad hoc dp values. */
object Spacing {
    val screen = 20.dp
    val cardInner = 16.dp
    val itemGap = 12.dp
    val rowGap = 8.dp
    val iconGap = 4.dp
}
